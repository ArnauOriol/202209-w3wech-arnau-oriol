export const pokemonApiUrl = "https://pokeapi.co/api/v2/pokemon/";
const listNumber = 10;
const offsetCounter = 0;
export const pokemonListLimit = `?limit=${listNumber}`;
export const offset = `&offset=${offsetCounter}`;
